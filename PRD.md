# 個人命令列天氣工具：產品需求書（PRD）

## 1. 產品概述

建立一個供個人使用的 Python 命令列天氣工具。使用者輸入地名後，可取得該行政區域的即時天氣；未輸入地名時，工具以目前公開 IP 位址推測所在城市。

工具以繁體中文為預設輸出語言，亦支援英文。Python 執行環境、套件與指令一律由 `uv` 管理。

## 2. 目標與非目標

### 目標

- 讓使用者以中文或英文地名快速取得即時天氣。
- 對台灣地名提供可靠的行政區解析，避免同名中國地點、商家或興趣點（POI）造成誤判。
- 未輸入地名時，提供合理但明確標示為「依 IP 推測」的城市天氣。
- 在 Windows 終端機正確顯示繁體中文與其他 Unicode 地名。

### 非目標

- 不提供逐小時／多日預報、氣象警報或雷達圖。
- 不支援地址、店家、景點等精確 POI 查詢；查詢目標是城市、縣市、行政區與聚落。
- 不保證 IP 定位精準到實體位置。
- 不設計為高流量、多使用者或批次地理編碼服務。

## 3. 使用者與使用情境

- 個人使用者在 PowerShell 或其他終端機中查詢所在地或旅行目的地天氣。
- 使用者可輸入 `台北`、`桃園`、`基隆`、`Tokyo`、`New York` 等地名。
- 使用者可不輸入地名，讓工具以公開 IP 推測城市。

## 4. 命令列介面

```powershell
uv run weather [LOCATION] [--lang zh-TW|en]
```

| 項目 | 說明 |
| --- | --- |
| `LOCATION` | 可省略；接受中文或英文行政區／城市名稱。多個字以空白組合為同一查詢。 |
| `--lang zh-TW` | 預設值；以繁體中文輸出。 |
| `--lang en` | 以英文輸出。 |
| `--version` | 顯示工具版本。 |

範例：

```powershell
uv run weather 基隆
uv run weather 桃園 --lang en
uv run weather "New York"
uv run weather
```

## 5. 功能需求

### FR-1：地名查詢

1. 系統必須接受中文與英文地名。
2. 系統必須將輸入解析為行政區、城市、縣市、國家層級區域或聚落；不得將商家、建物、車站、理髮廳等 POI 作為結果。
3. 找不到地點時，系統必須顯示與目前輸出語言一致的明確錯誤訊息，並以非零狀態碼結束。
4. 系統必須顯示解析後使用的地點名稱與國家／地區，方便使用者察覺歧義。

### FR-2：台灣行政區優先與同名消歧

1. 對含中文的查詢，若存在相符的台灣行政區或聚落，系統必須優先採用台灣結果。
2. 系統必須支援台灣六都、其他縣市，以及澎湖、金門、馬祖（連江縣）。
3. 系統必須以台灣官方行政區名稱進行限定查詢，處理同名或資料來源不完整的情況。
4. 系統不得因「桃園」等同名地名而回傳中國地點；不得因「波士頓」等查詢而回傳台灣商家。
5. 當使用者明確輸入非台灣的英文城市時，系統必須採用對應的全球城市結果。

### FR-3：IP 城市推測

1. 當未提供 `LOCATION` 時，系統必須透過 IP 定位服務取得城市與座標。
2. 輸出必須標示此結果為「依 IP 推測」／`Estimated from IP`。
3. IP 定位失敗時，系統必須提示使用者直接輸入地名，不得改用任意預設城市。

### FR-4：即時天氣輸出

1. 系統必須依解析後的緯度、經度取得即時天氣。
2. 輸出至少包含：天氣狀況、日／夜、地點、觀測時間、氣溫、體感溫度、相對濕度、降水量、風速與雲量。
3. 天氣代碼必須依輸出語言轉換為繁體中文或英文描述。
4. 時間必須使用查詢地點的時區。
5. 預設輸出必須為繁體中文，且 Windows 終端機應以 UTF-8 輸出，避免中文或國際字元編碼失敗。

### FR-5：錯誤處理

1. 地理編碼、IP 定位或天氣服務無法連線時，系統必須以使用者可理解的訊息說明並以非零狀態碼結束。
2. 錯誤訊息須依 `--lang` 顯示。
3. 系統不得將服務暫時限流誤報為「找不到地名」。

## 6. 外部服務與整合需求

| 用途 | 服務 | 要求 |
| --- | --- | --- |
| 即時天氣 | Open-Meteo Forecast API | 依緯度與經度查詢目前天氣及地點時區。 |
| 優先地理編碼 | Open-Meteo Geocoding API | 用於全球城市與英文地名查詢。 |
| 中文／台灣地名備援 | OpenStreetMap Nominatim | 僅查詢行政區或聚落類別，並以台灣國別限制處理台灣中文地名。 |
| 未輸入地名定位 | ipapi.co | 取得公開 IP 推測的城市與座標。 |

### Nominatim 使用限制

本產品使用公開 Nominatim 端點時，必須遵守其政策：

1. 請求頻率絕對不得超過每秒 1 次。
2. 必須傳送可識別應用程式的 `User-Agent`；不得使用 HTTP 函式庫的預設識別字串。
3. 必須在 README、說明或適當輸出位置標示 OpenStreetMap／Nominatim 資料來源。
4. 不得實作自動完成、系統性掃描、POI 大量抓取或其他批次地理編碼功能。
5. 若進行一次性批次驗證，必須單執行緒、每秒至多一次、快取結果；長時間或定期批次工作應降至每分鐘最多 4 次。
6. 正常終端機查詢必須由使用者直接觸發；不可在背景定期查詢。
7. 程式設計應保留日後切換地理編碼供應商的能力。

政策來源：[Nominatim Usage Policy](https://operations.osmfoundation.org/policies/nominatim/)。

## 7. 非功能需求

### 技術與相容性

- Python 版本：3.11 以上。
- 套件、虛擬環境、鎖檔與執行命令：一律使用 `uv`。
- 工具應盡量維持少量依賴；可使用 Python 標準函式庫處理 HTTP 與 CLI。
- 必須支援 Windows PowerShell；輸出必須能顯示 UTF-8 文字。

### 效能與可靠性

- 單次查詢應只發送達成目的所需的最少請求。
- 對 Nominatim 查詢應實作節流與查詢結果快取，避免重複查詢相同地名。
- 發生限流或暫時性網路錯誤時，應提示稍後重試；不得自動快速重送。
- API 回傳缺少必要欄位時，應安全失敗並顯示服務資料無法使用。

### 隱私

- IP 定位僅使用使用者當下公開 IP 取得的大略城市資訊。
- 不得儲存 IP 位址、查詢歷史或精確座標，除非未來另行取得使用者授權。

## 8. 驗收標準

### 台灣地名

下列查詢皆須回傳台灣，且不可回傳商家或中國同名地點：

- 六都：臺北、新北、桃園、臺中、臺南、高雄。
- 其他縣市：基隆、新竹市、新竹縣、嘉義市、嘉義縣、宜蘭、苗栗、彰化、南投、雲林、屏東、臺東、花蓮。
- 離島：澎湖、金門、馬祖。

### 全球代表城市

下列查詢須解析至正確國家：

- 亞洲：Tokyo、Seoul、Singapore、New Delhi、Bangkok。
- 歐洲：London、Paris、Berlin、Rome、Madrid。
- 美洲：New York、Toronto、Mexico City、Sao Paulo、Buenos Aires。

### 行為驗收

1. `uv run weather 波士頓` 的結果為美國波士頓，不得為台灣理髮廳或其他 POI。
2. `uv run weather` 的結果含有「依 IP 推測」標示。
3. `uv run weather "New York" --lang en` 以英文輸出。
4. 找不到地名、網路失敗與限流時，皆輸出正確語言的明確錯誤訊息並回傳非零狀態碼。
5. 測試不得以高頻批次呼叫公開 Nominatim；測試間隔與快取策略必須符合其使用政策。

## 9. 後續建議

- 將地名解析結果加上本機快取與每秒一次的節流器。
- 將「服務暫時限流」與一般網路中斷分開顯示。
- 若使用人數或查詢頻率提高，改用符合需求的商業地理編碼服務或自建 Nominatim 實例。
